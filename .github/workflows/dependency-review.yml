name: Dependency Review

on:
  pull_request:

permissions:
  contents: read
  pull-requests: read

jobs:
  dependency-review:
    name: Dependency Review
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          fetch-depth: 0

      - uses: actions/dependency-review-action@2031cfc080254a8a887f58cffee85186f0e49e48 # v4.9.0
        with:
          # Current Tauri/Wry Linux builds still transitively depend on gtk-rs 0.18/glib < 0.20.
          # Track removal of this exception once upstream migrates to the patched glib line.
          allow-ghsas: GHSA-wrw7-89jp-8q8g
          show-patched-versions: true
