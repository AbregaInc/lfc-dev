{
  "name": "@lfc/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:prod": "wrangler deploy --env production",
    "lint": "tsc --noEmit",
    "db:migrate:local": "wrangler d1 migrations apply lfc-db --local",
    "db:migrate": "wrangler d1 migrations apply lfc-db --remote",
    "db:migrate:prod": "wrangler d1 migrations apply lfc-db --remote --env production"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "hono": "^4.12.15",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260507.1",
    "typescript": "^6.0.3",
    "wrangler": "^4.85.0"
  }
}
